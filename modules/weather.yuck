(defwidget weather []
  (box
    :space-evenly false
    :orientation 'v'
    :valign 'center'
    :class 'weather-module'
    (box 
      :space-evenly false
      :orientation 'h'
      :halign 'center'
      :class 'weather'
      (label 
        :halign 'start' 
        :class 'weather-temperature' 
        :text '${weather-temperature}°C'
      )
      (label 
        :class 'weather-icon' 
        :text '${weather-icon}'
      )
    )
  )
)
